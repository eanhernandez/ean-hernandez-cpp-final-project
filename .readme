this version now starts with command line arguments,
and will distribute to n clients, or just run on one
server.  I've noticed that above 500 queries, faults 
tend to occur, particularly with the compiled .exe 
version.